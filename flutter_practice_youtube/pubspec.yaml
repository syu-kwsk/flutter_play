name: flutter_practice_youtube
description: A new Flutter application.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  provider: ^4.3.2+2
  firebase_core: ^0.5.2
  cloud_firestore: ^0.14.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
